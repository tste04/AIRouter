---
name: Bug melden
about: Ein reproduzierbares Fehlverhalten des Routers
labels: bug
---

**Was passiert, was sollte passieren?**

**Reproduktion** (Konfiguration + Aufruf; gern als Test gegen `MockTransport`):

```swift
```

**Umgebung**: AIRouter-Commit/Version, macOS/iOS-Version, Xcode/Swift-Version.

**Relevante Ausgabe** (`AIRouterError`, `budgetStatus()`, `healthStatus()`,
Log-Auszug — bitte ohne Credentials/Prompts mit sensiblen Inhalten):
